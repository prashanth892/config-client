---
management:
  endpoints:
    web:
      exposure:
        include: refresh
  security:
    enabled: false
spring:
  application:
    name: config-server-client
  cloud:
    config:
      uri: "http://localhost:8888"
  profiles:
    active: default
